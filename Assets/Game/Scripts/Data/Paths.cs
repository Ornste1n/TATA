using UnityEngine;

namespace Game.Scripts.Data
{
    public static class Paths
    {
        public static readonly string Options = Application.persistentDataPath + "/options.dat";
        public static readonly string Graphics = Application.persistentDataPath + "/config.txt";
    }
}